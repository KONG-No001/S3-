<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gp.dao.PurchaseGoodsDao">

    <insert id="add">
        INSERT INTO `mydb`.`purchase_goods`
        (`purchase_id`,
        `goods_id`)
        VALUES
        <foreach collection="goodsIds" item="goodsId" separator=",">
            (#{purchaseId},#{goodsId})
        </foreach>
    </insert>

    <delete id="deleteByPurchaseId">
        delete from purchase_goods where purchase_id = #{id}
    </delete>


</mapper>
